$(document).ready(function() {
    // Auto Init Components
    M.AutoInit();

    // // Scrollspy Init
    // $('.scrollspy').scrollSpy({
    //     scrollOffset: -30
    // });

    // // Init modal
    // $('.section_3 .container .vid-modal').modal();
});